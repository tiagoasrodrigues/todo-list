import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    backgroundColor: '#0d0d0d',

    height: 173,
    color: '#4ea8de',
    textAlign: 'center',
    paddingTop: 70,
    alignItems: 'center',
  },
})